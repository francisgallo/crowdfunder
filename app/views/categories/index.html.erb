<h1>Categories</h1>

<% @categories.each do |category| %>

<p>
  <%= link_to category.name, project_path(category.id) %>
</p>

<% end %>

<!-- not sure this is linking us to where we actually want to go... -->
